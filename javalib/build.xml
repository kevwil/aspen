<?xml version="1.0" encoding="UTF-8"?>
<project name="aspenj" default="jarjar">

  <target name="jarjar" description="merge jars into one">
    <zip destfile="../lib/${ant.project.name}.jar">
      <zipgroupfileset dir="target" includes="*.jar"/>
    </zip>
  </target>

</project>